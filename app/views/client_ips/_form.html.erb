<%= form_with(model: client_ip) do |form| %>
  <% if client_ip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(client_ip.errors.count, "error") %> prohibited this client_ip from being saved:</h2>

      <ul>
        <% client_ip.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
